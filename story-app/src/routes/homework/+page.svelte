<div class="hw-stage">

  <div class="hw-side hw-side-left">
    <div class="hw-sub">Which of the three pillars feels weakest for partners today, and why?</div>
    <div class="hw-sub">Pick one role from the list that you think is most underserved by current enablement. Why?</div>
    <div class="hw-sub">Does one enablement program serve both partners, or do they need fundamentally different things?</div>
  </div>

  <div class="hw-card">
    <div class="hw-eyebrow">Homework</div>
    <div class="hw-question">
      If you could fix <em>ONE</em> thing about how Anaplan enables its partners today, what would it be and why?
    </div>
  </div>

  <div class="hw-side hw-side-right">
    <div class="hw-sub">If you were a customer selecting a partner, what would you want to see?</div>
    <div class="hw-sub">Of the metrics above, which one do we NOT measure today but probably should?</div>
  </div>

</div>

<style>
  .hw-stage {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 32px;
    height: 100%;
    padding: 40px 36px;
  }

  .hw-card {
    background: white;
    border: 1px solid var(--color-rule);
    border-left: 6px solid var(--color-orange);
    border-radius: 0 12px 12px 0;
    padding: 40px 48px;
    max-width: 520px;
    width: 100%;
    flex-shrink: 0;
    display: flex;
    flex-direction: column;
    gap: 16px;
  }

  .hw-eyebrow {
    font-size: 10px;
    font-weight: bold;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--color-orange);
  }

  .hw-question {
    font-size: 24px;
    font-weight: bold;
    color: var(--color-ink);
    line-height: 1.4;
    letter-spacing: -0.01em;
  }

  .hw-question em {
    font-style: normal;
    color: var(--color-orange);
  }

  .hw-side {
    display: flex;
    flex-direction: column;
    gap: 22px;
    width: 240px;
    flex-shrink: 0;
  }

  .hw-sub {
    font-size: 14px;
    font-weight: 600;
    color: var(--color-muted);
    line-height: 1.55;
    padding-left: 12px;
    border-left: 2px solid var(--color-rule);
  }
</style>
