<div class="intro-stage">
  <div class="intro-header">
    <div class="intro-eyebrow">Connected Enablement</div>
    <div class="intro-title">Three pillars that drive partner success</div>
    <div class="intro-subtitle">Technical expertise drives delivery excellence. Delivery excellence drives pipeline growth.</div>
  </div>

  <div class="intro-pillars">
    <div class="intro-pillar">
      <div class="intro-pillar-icon">🎓</div>
      <div class="intro-pillar-name">Technical Expertise</div>
      <div class="intro-pillar-desc">
        Build deep, certified capability across model building, application configuration, solution architecture, and the Anaplan platform.
      </div>
      <div class="intro-callout">🏆 Win rates</div>
    </div>

    <div class="intro-arrow">→</div>

    <div class="intro-pillar">
      <div class="intro-pillar-icon">⭐</div>
      <div class="intro-pillar-name">Delivery Excellence</div>
      <div class="intro-pillar-desc">
        Ensure every client engagement meets Anaplan's quality bar and client expectations — from supervised engagements through independent delivery.
      </div>
      <div class="intro-callout">⭐ Delivery quality</div>
    </div>

    <div class="intro-arrow">→</div>

    <div class="intro-pillar">
      <div class="intro-pillar-icon">📈</div>
      <div class="intro-pillar-name">Pipeline Growth</div>
      <div class="intro-pillar-desc">
        Equip partner sales and pre-sales teams to identify, qualify, position, present, and close Anaplan opportunities with confidence across every product and industry.
      </div>
      <div class="intro-callout">Increase pipeline</div>
    </div>
  </div>
</div>

<style>
  .intro-stage {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    height: 100%;
    padding: 32px 40px;
    gap: 32px;
  }

  .intro-header {
    text-align: center;
  }

  .intro-eyebrow {
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--color-orange);
    margin-bottom: 8px;
  }

  .intro-title {
    font-size: 28px;
    font-weight: 700;
    color: var(--color-ink);
    letter-spacing: -0.02em;
    margin-bottom: 8px;
  }

  .intro-subtitle {
    font-size: 14px;
    color: var(--color-muted);
    max-width: 560px;
    margin: 0 auto;
    line-height: 1.5;
  }

  .intro-pillars {
    display: flex;
    align-items: stretch;
    gap: 0;
    width: 100%;
    max-width: 900px;
  }

  .intro-arrow {
    font-size: 24px;
    color: var(--color-orange);
    display: flex;
    align-items: center;
    padding: 0 12px;
    flex-shrink: 0;
  }

  .intro-pillar {
    flex: 1;
    background: white;
    border: 1px solid var(--color-rule);
    border-radius: 10px;
    padding: 28px 24px 20px;
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .intro-pillar-icon {
    font-size: 28px;
    line-height: 1;
  }

  .intro-pillar-name {
    font-size: 16px;
    font-weight: 700;
    color: var(--color-ink);
  }

  .intro-pillar-desc {
    font-size: 13px;
    color: var(--color-muted);
    line-height: 1.55;
    flex: 1;
  }

  .intro-callout {
    margin-top: 4px;
    align-self: flex-start;
    font-size: 12px;
    font-weight: 700;
    color: var(--color-orange);
    background: rgba(255, 97, 0, 0.07);
    border: 1px solid rgba(255, 97, 0, 0.2);
    border-radius: 20px;
    padding: 4px 12px;
  }
</style>
